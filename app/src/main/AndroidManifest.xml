<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.main.fitness">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fitness">
        <activity
            android:name=".ui.activities.RunningMapsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ScheduleActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.MyRunRecordsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.WorkoutSessionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ExerciseDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.activities.ExerciseBankActivity" />
        <activity
            android:name=".ui.activities.ProgramDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ProgramListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ExerciseBankActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.FAQAndTerminologyActivity"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MyFirebaseMessagingService"
            android:enabled="true"
            android:exported="true" />

        <service android:name=".service.MyLocationUpdateService"
            android:foregroundServiceType="location"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true" />
    </application>

</manifest>